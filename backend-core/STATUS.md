# Backend — Person 1 (Khushi)

**Status: Not included in this merge.**

## Planned scope (from team roster)

- Python + FastAPI
- PostgreSQL + PostGIS
- Core REST APIs
- Database schema and migrations
- Authentication

This folder is a placeholder so the repository structure reflects the full
project plan (Team 1 — Backend + GIS/Routing). It will hold the API layer
that:

- Serves risk scores from `intelligence-data/ml-risk-engine`
- Serves cleaned/simulated data from `intelligence-data/data-integration`
- Is consumed by `dashboard-frontend` (via `src/services/api.js`) and,
  eventually, `field-app`
